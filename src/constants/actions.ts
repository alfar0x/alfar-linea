export const ACTION_LEND = "lend";
export const ACTION_SWAP = "swap";
export const ACTION_RANDOM = "random";

const ACTIONS = [ACTION_LEND, ACTION_SWAP, ACTION_RANDOM] as const;

export default ACTIONS;
